# Backend Production Environment Variables

# MongoDB Connection
# Replace with your actual MongoDB URI
MONGODB_URI=mongodb+srv://viradiyakrish13:viradiyakrish13@cluster0.g1a9v.mongodb.net/

# JWT Secret (use a strong, unique value in production)
JWT_SECRET=task-management-system-production-secret

# Frontend URL (Vercel deployment URL for frontend)
FRONTEND_URL=https://task-management-frontend-beta-ivory.vercel.app

# Email Configuration (if using email notifications)
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password

# Optional: Logging
LOG_LEVEL=info

# Node Environment
NODE_ENV=production

# Note: You'll need to set these values in Vercel's environment variables
# when deploying the backend. This file serves as a template and the values
# should be set in the Vercel dashboard.